import React from 'react';
import ChatContainer from '../components/chat/ChatContainer';

const Home = () => {
    return (
        <div style={{ height: '100%', maxWidth: '1000px', margin: '0 auto', width: '100%' }}>
            <ChatContainer />
        </div>
    );
};

export default Home;
